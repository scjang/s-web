general:
  branches:
    only: 
      - master
machine:
  node:
    version: 4.4.5
dependencies:
  override:
    - echo "no dependencies"
test:
  post:
    - echo "no test"
deployment:
  staging:
    branch: master
    commands:
      - exit